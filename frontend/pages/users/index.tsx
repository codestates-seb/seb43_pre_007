import styled from 'styled-components';

//경로 https://stackoverflow.com/users
const Users = () => {
  return <UsersContainer>유저페이지입니다.</UsersContainer>;
};

export default Users;

const UsersContainer = styled.div`
  padding: 24px;
`;